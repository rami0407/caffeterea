# 🚀 نشر التعديلات على الموقع (Deployment)

لقد قمنا بتجهيز ملفات الإعداد (`firebase.json` و `.firebaserc`). بما أن أداة `firebase` غير متاحة في التيرمينال الحالي، يرجى تنفيذ الخطوات التالية يدوياً لرفع التعديلات:

1. افتح التيرمينال (Command Prompt أو PowerShell) في مجلد المشروع:
   `c:\Users\ramia\Downloads\files (1)\caffeterea`

2. تأكد من تسجيل دخولك (إذا لم تكن مسجلاً):
   ```bash
   firebase login
   ```

3. **لرفع ملفات الموقع (HTML/JS) وقواعد البيانات (Rules) معاً، نفذ:**
   ```bash
   firebase deploy
   ```

4. إذا كنت تريد رفع القواعد فقط (لإصلاح مشكلة الصلاحيات فوراً):
   ```bash
   firebase deploy --only firestore:rules
   ```

5. إذا كنت تريد رفع ملفات الموقع فقط:
   ```bash
   firebase deploy --only hosting
   ```

---
**ملاحظة:**
تأكدنا من أن ملف `firebase.json` يشير بشكل صحيح إلى ملف القواعد `firestore.rules`، لذا الأمر `firebase deploy` سيقوم بتحديث كل شيء دفعة واحدة.
